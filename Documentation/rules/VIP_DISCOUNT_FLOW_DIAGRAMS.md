# VIP Discount System - Flow Diagram

## Order Creation to Discount Application Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. VIP CUSTOMER CREATES ORDER                                  │
│    - POST /orders/create                                       │
│    - Order saved to database                                   │
│    - Status: PENDING                                           │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. DJANGO SIGNAL FIRES                                          │
│    - post_save signal on Order model                           │
│    - orders/signals.py::process_rules_on_order_creation()     │
│    - Created=True, so rule processing triggered               │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. RULE ENGINE INITIALIZATION                                  │
│    - process_rules(order, 'discount_vip')                      │
│    - rules/engine.py::process_rules()                          │
│    - Fetches RuleTrigger with code='discount_vip'             │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. RULE FILTERING                                              │
│    - Find all ENABLED rules with 'discount_vip' trigger        │
│    - Filter by customer labels (if rule has label constraints) │
│    - Result: [VIP Order Discount Rule]                         │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. CONDITION EVALUATION                                        │
│    - For each rule, evaluate all conditions                    │
│    - Condition: Entity=CUSTOMER, Operator=HAS_LABEL,          │
│                 Label=Customer_VIP                             │
│    - Get customer labels from order.customer.labels.all()      │
│    - Check if 'Customer_VIP' label exists                      │
│    - Result: True (customer is VIP)                            │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. ACTION EXECUTION                                            │
│    - All conditions met → execute actions                      │
│    - execute_action(RuleAction, order)                         │
│    - Action Type: DISCOUNT                                     │
│    - Action Params: {"percentage": 10,                         │
│                      "description": "VIP New Order..."}        │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. DISCOUNT CALCULATION & APPLICATION                          │
│    - Set order.discount_percentage = 10                        │
│    - Set order.discount_description = "VIP New Order..."       │
│    - Call order.calculate_total()                              │
│      ├─ Calculate line items subtotal                          │
│      ├─ Calculate discount: subtotal * (10 / 100)             │
│      ├─ Set order.discount_amount = calculated discount        │
│      └─ Set order.total_amount = subtotal - discount          │
│    - Save order with all discount fields                       │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 8. ORDER UPDATED IN DATABASE                                   │
│    ┌──────────────────────────────────────────────────────┐   │
│    │ ORDER TABLE UPDATE:                                  │   │
│    │ ├─ discount_percentage = 10.00                       │   │
│    │ ├─ discount_amount = 50.00 (€)                      │   │
│    │ ├─ discount_description = "VIP New Order Discount"   │   │
│    │ └─ total_amount = 450.00 (€) [500 - 50]            │   │
│    └──────────────────────────────────────────────────────┘   │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 9. CUSTOMER VIEWS BASKET                                       │
│    - GET /orders/basket                                        │
│    - BasketView.get_context_data() loads order                │
│    - Passes discount data to template                          │
│    ├─ discount_percentage = 10.00                             │
│    ├─ discount_amount = 50.00                                 │
│    ├─ discount_description = "VIP New Order Discount"         │
│    └─ grand_total = 450.00                                    │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│ 10. DISPLAY DISCOUNT IN TEMPLATE                               │
│     ┌───────────────────────────────────────────────────────┐ │
│     │ 🎉 VIP Discount Applied!                            │ │
│     │ VIP New Order Discount: -10% (€50.00)              │ │
│     │                                                     │ │
│     │ Grand Total: €450.00                               │ │
│     └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## Component Interaction Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                    orders/signals.py                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ @receiver(post_save, sender=Order)                       │  │
│  │ def process_rules_on_order_creation():                   │  │
│  │   if created:                                            │  │
│  │     process_rules(order, 'discount_vip')                │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────┬──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────────────┐
│              rules/engine.py::process_rules()                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Fetch RuleTrigger by code                             │  │
│  │ 2. Filter ENABLED rules with this trigger                │  │
│  │ 3. Check customer labels for rule label filters          │  │
│  │ 4. For each applicable rule:                             │  │
│  │    - Evaluate all conditions                             │  │
│  │    - If all true, execute all actions                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────┬──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────────────┐
│          rules/engine.py::execute_action()                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ if action.action_type == 'DISCOUNT':                     │  │
│  │   - Extract percentage and description                   │  │
│  │   - Set order.discount_percentage                        │  │
│  │   - Set order.discount_description                       │  │
│  │   - Call order.calculate_total()                         │  │
│  │   - Save order                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────┬──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────────────┐
│         orders/models.py::Order.calculate_total()                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Sum all OrderItem amounts                             │  │
│  │ 2. if discount_percentage > 0:                           │  │
│  │    - discount_amount = subtotal * (% / 100)             │  │
│  │    - total = subtotal - discount_amount                  │  │
│  │ 3. Set self.total_amount = total                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────┬──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────────────┐
│         orders/views.py::BasketView                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ get_context_data():                                      │  │
│  │   - Load current order                                   │  │
│  │   - Pass discount_percentage to template                 │  │
│  │   - Pass discount_amount to template                     │  │
│  │   - Pass discount_description to template                │  │
│  │   - Pass grand_total (after discount) to template        │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────┬──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────────────┐
│      orders/templates/orders/basket.html                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ {% if discount_percentage > 0 %}                         │  │
│  │   <div class="alert alert-success">                      │  │
│  │     🎉 VIP Discount Applied!                           │  │
│  │     {{ discount_description }}: -{{ percentage }}%      │  │
│  │     ({{ discount_amount }})                              │  │
│  │   </div>                                                 │  │
│  │ {% endif %}                                              │  │
│  │ Grand Total: {{ grand_total }}                           │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

## Database Schema Changes

```
ORDERS_ORDER TABLE
┌─────────────────────────────────────┐
│ id (PK)                             │
│ customer_id (FK)                    │
│ total_amount                        │
│ status                              │
│ ...                                 │
│ NEW: discount_percentage (DECIMAL)  │  ← Store discount %
│ NEW: discount_amount (DECIMAL)      │  ← Store discount amount
│ NEW: discount_description (VARCHAR) │  ← Store discount reason
└─────────────────────────────────────┘
        ▲
        │ (relationship)
        │
┌─────────────────────────────────────┐
│ USERS_CUSTOMER TABLE                │
│ ├─ id (PK)                          │
│ ├─ user_id (FK)                     │
│ └─ labels (M2M through junction)    │
└─────────────────────────────────────┘
        ▲
        │ (M2M relationship)
        │
┌─────────────────────────────────────┐
│ LABELS_LABEL TABLE                  │
│ ├─ id (PK)                          │
│ ├─ name (VARCHAR)                   │
│ ├─ label_type (VARCHAR)             │
│ └─ ...                              │
│                                     │
│ Data: Customer_VIP label            │
│ - name = "Customer_VIP"             │
│ - label_type = "CUSTOMER"           │
└─────────────────────────────────────┘
```

## Admin Configuration Flow

```
DJANGO ADMIN SETUP
├─ Labels / Create Label
│  ├─ name: "Customer_VIP"
│  └─ label_type: "CUSTOMER"
│
├─ Rules / Rule Triggers / Create
│  ├─ name: "On Order Creation Trigger"
│  └─ code: "discount_vip"
│
├─ Rules / Rules / Create
│  ├─ name: "VIP Order Discount Rule"
│  ├─ status: "ENABLED"
│  └─ trigger: "On Order Creation Trigger"
│
├─ Rules / Rule Conditions / Create
│  ├─ rule: "VIP Order Discount Rule"
│  ├─ entity: "CUSTOMER"
│  ├─ operator: "HAS_LABEL"
│  └─ label: "Customer_VIP"
│
└─ Rules / Rule Actions / Create
   ├─ rule: "VIP Order Discount Rule"
   ├─ action_type: "DISCOUNT"
   └─ action_params: {"percentage": 10, "description": "VIP New Order Discount"}
```

## Customer Setup to Discount Application

```
┌─ VIP Customer Setup
│  ├─ Create User account
│  ├─ Create Customer profile
│  └─ Assign "Customer_VIP" label to customer.labels
│
├─ Customer Creates Order
│  ├─ POST /orders/create
│  ├─ Order saved (discount_percentage=0 initially)
│  └─ Signal fires → Rule processing starts
│
├─ Rule Engine Processes
│  ├─ Checks: Is customer VIP? (checks for label)
│  ├─ Evaluates: All conditions met? (YES)
│  └─ Executes: DISCOUNT action
│
├─ Discount Applied
│  ├─ Sets discount_percentage = 10
│  ├─ Calculates discount_amount
│  ├─ Reduces total_amount by discount
│  └─ Saves to database
│
└─ Customer Views Basket
   ├─ Sees discount alert
   ├─ Sees final total (reduced)
   └─ Proceeds to checkout
```
